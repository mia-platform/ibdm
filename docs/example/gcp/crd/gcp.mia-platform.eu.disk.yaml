---
# yaml-language-server: $schema=https://cdn.mia-platform.eu/catalog/v1alpha1/mia-platform.eu.v1alpha1.CustomResourceDefinition.json
apiVersion: mia-platform.eu/v1alpha1
kind: CustomResourceDefinition
metadata:
  name: disks.gcp.mia-platform.eu
spec:
  group: gcp.mia-platform.eu
  names:
    kind: Disk
    plural: disks
    singular: disk
  scope: Organization
  versions:
    - deprecated: false
      name: v1alpha1
      schema:
        openAPIV31Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                id:
                  description: Unique identifier for the disk resource
                  type: string
                name:
                  description: The user-assigned name of the disk
                  type: string
                architecture:
                  description: CPU architecture the disk image or snapshot targets
                  type: string
                enableConfidentialCompute:
                  description: Whether the disk is configured for Confidential Compute
                  type: boolean
                physicalBlockSizeBytes:
                  description: Physical block size of the persistent disk, in bytes
                  type: integer
                  format: int64
                sizeGb:
                  description: Size of the disk in gigabytes
                  type: integer
                status:
                  description: Current status of the disk (for example, READY)
                  type: string
                updateTime:
                  description: RFC 3339 timestamp for the last update to the disk resource
                  type: string
                  format: date-time
                location:
                  description: The zone or region where the disk resides
                  type: string
              additionalProperties: true
      served: true
      storage: true
