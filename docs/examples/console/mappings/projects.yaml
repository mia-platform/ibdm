apiVersion: console.mia-platform.eu/v1alpha1
resource: projects
type: console.mia-platform.eu/Project
syncable: true
mappings:
  identifier: |-
    {{ $name := .name -}}
    {{- $name = trimPrefix "//console.mia-platform.eu/" $name -}}
    {{- replace "_" "-" $name }}
  spec:
    name: "{{ .name }}"
    created: "{{ .timeCreated }}"
    lastUpdate: "{{ .updated }}"
    labels: "{{ .labels | toJSON }}"
