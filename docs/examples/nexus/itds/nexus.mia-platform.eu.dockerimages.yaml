# yaml-language-server: $schema=https://cdn.mia-platform.eu/catalog/v1alpha1/mia-platform.eu.v1alpha1.ItemTypeDefinition.json
---
apiVersion: mia-platform.eu/v1alpha1
kind: ItemTypeDefinition
metadata:
  name: dockerimages.nexus.mia-platform.eu
spec:
  group: nexus.mia-platform.eu
  names:
    kind: DockerImage
    plural: dockerimages
    displayPlural: Docker Images
  scope: Organization
  versions:
    - deprecated: false
      name: v1alpha1
      schema:
        openAPIV31Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                host:
                  description: The registry host of the Docker image.
                  type: string
                  title: Host
                name:
                  description: The name of the Docker image.
                  type: string
                  title: Name
                version:
                  description: The version tag of the Docker image.
                  type: string
                  title: Version
                repository:
                  description: The repository containing this Docker image.
                  type: string
                  title: Repository
                format:
                  description: The component format (e.g. docker).
                  type: string
                  title: Format
                tags:
                  description: Tags associated with this Docker image.
                  type: array
                  title: Tags
                  additionalProperties: true
                assets:
                  description: The list of assets belonging to this Docker image component.
                  type: array
                  title: Assets
                  items:
                    type: object
                    properties:
                      sha256:
                        description: SHA-256 checksum of the asset.
                        type: string
                        title: SHA-256
                      downloadUrl:
                        description: URL to download the asset.
                        type: string
                        title: Download URL
                      lastModified:
                        description: Timestamp of the last modification to the asset.
                        type: string
                        format: date-time
                        title: Last Modified
                      lastDownloaded:
                        description: Timestamp of the last download of the asset.
                        type: string
                        format: date-time
                        title: Last Downloaded
      served: true
      storage: true
