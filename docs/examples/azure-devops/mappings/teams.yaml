apiVersion: azuredevops.mia-platform.eu/v1alpha1
itemFamily: teams
type: team
syncable: true
mappings:
  identifier: "{{ .url | sha256sum }}"
  spec:
    name: "{{ .name }}"
    projectName: "{{ .projectName }}"
    description: "{{ get \"description\" . \"\" | quote }}"
