# yaml-language-server: $schema=https://cdn.mia-platform.eu/catalog/v1alpha1/miaplatform.eu.v1alpha1.CustomResourceDefinition.json
---
apiVersion: mia-platform.eu/v1alpha1
kind: CustomResourceDefinition
metadata:
  name: folders.gcp.mia-platform.eu
spec:
  group: gcp.mia-platform.eu
  names:
    kind: Folder
    plural: folders
    singular: folder
  scope: Organization
  versions:
  - deprecated: false
    name: v1alpha1
    schema:
      openAPIV31Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              name:
                description: The folder id used to identify the folder in GCP
                type: string
              displayName:
                description: The user visible name for the project.
                type: string
              parent:
                description: The parent resource identifier (for example, 'folders/{folder_id}' or 'organizations/{org_id}')
                type: string
              state:
                description: Current lifecycle state of the folder. Possible values are 'state_unspecified', 'active', or 'delete_requested'.
                type: string
              createTime:
                description: RFC 3339 timestamp for when the folder was created
                type: string
                format: date-time
              labels:
                description: User-provided labels in key-value pairs.
                type: object
                additionalProperties:
                  type: string
    served: true
    storage: true
