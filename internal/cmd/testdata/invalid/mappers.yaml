type: valid
apiVersion: v1
resource: resource
mappings:
  identifier: "{{ .id }}"
  spec:
    field1: "{{ .field1 }}"
---
type: mapper-type
apiVersion: v1
resource: resource
mappings:
  identifier: "{{ .id }}"
  spec:
    field1: "{{ .field1 | invalidFunc }}"
