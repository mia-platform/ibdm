type: valid
apiVersion: v1
itemFamily: family
mappings:
  identifier: "{{ .id }}"
  spec:
    field1: "{{ .field1 }}"
---
type: mapper-type
apiVersion: v1
itemFamily: family
syncable: true
mappings:
  identifier: "{{ .id }}"
  spec:
    field1: "{{ .field1 | lower }}"
