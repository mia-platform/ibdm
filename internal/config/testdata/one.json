{
	"apiVersion": "group/v1",
	"itemFamily": "configs",
	"type": "json",
	"syncable": true,
	"mappings": {
		"identifier": "{{ .name }}",
		"spec": {
			"key": "{{ .value }}",
			"otherKey": "{{ .otherValue | functionName }}"
		}
	}
}
