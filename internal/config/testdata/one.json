{
	"type": "json",
	"apiVersion": "group/v1",
	"kind": "configs",
	"syncable": true,
	"mappings": {
		"identifier": "{{ .name }}",
		"spec": {
			"key": "{{ .value }}",
			"otherKey": "{{ .otherValue | functionName }}"
		}
	}
}
