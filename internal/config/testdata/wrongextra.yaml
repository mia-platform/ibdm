type: yaml
apiVersion: group/v1
itemFamily: configs
syncable: true
mappings:
  identifier: "{{ .name }}"
  spec:
    key: "{{ .value }}"
    otherKey: "{{ .otherValue | functionName }}"
  extra:
    - deletePolicy: "test"
